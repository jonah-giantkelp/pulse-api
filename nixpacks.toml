[variables]
NIXPACKS_POETRY_VERSION = "2.1.1"

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install poetry==2.1.1 && poetry install --without dev --no-interaction --no-ansi"
]
